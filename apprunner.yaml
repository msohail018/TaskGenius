version: 1.0
runtime: nodejs18
build:
  commands:
    build:
      - npm run install:all
      - npm run build:frontend
run:
  command: npm start
  network:
    port: 8080
  env:
    - name: NODE_ENV
      value: production
