version: 1.0
runtime: nodejs18
build:
  commands:
    build:
      - npm install
      - npm run deploy:build
run:
  command: npm start
  network:
    port: 5000
  env:
    - name: NODE_ENV
      value: production
